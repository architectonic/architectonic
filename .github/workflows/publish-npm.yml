# Publish architectonic via npm trusted publishing (OIDC).
# npm: Package → Settings → Trusted publishing → GitHub Actions →
#   repo architectonic/architectonic, workflow publish-npm.yml
name: Publish npm

on:
  push:
    branches:
      - main
    tags:
      - 'v[0-9]*.[0-9]*.[0-9]*'
    paths:
      - 'package.json'
      - 'README.md'
      - 'docs/**'
      - 'bin/**'
      - '.github/workflows/publish-npm.yml'
  workflow_dispatch:

concurrency:
  group: npm-publish-${{ github.ref }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Tag matches package version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          VER="$(node -p "require('./package.json').version")"
          if [ "$VER" != "$TAG" ]; then
            echo "version mismatch: package.json is $VER but tag is v$TAG"
            exit 1
          fi
          echo "OK: architectonic at $TAG"

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Smoke test
        run: node bin/architectonic.js --help

      - name: npm CLI (trusted publishing needs >= 11.5.1)
        run: npm install -g npm@latest

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Check registry version
        id: registry
        run: |
          VER="$(node -p "require('./package.json').version")"
          CURRENT="$(npm view architectonic version 2>/dev/null || true)"
          echo "package_version=$VER" >> "$GITHUB_OUTPUT"
          echo "registry_version=$CURRENT" >> "$GITHUB_OUTPUT"
          if [ "$CURRENT" = "$VER" ]; then
            echo "already_published=true" >> "$GITHUB_OUTPUT"
            echo "architectonic@$VER is already published; skipping publish"
          else
            echo "already_published=false" >> "$GITHUB_OUTPUT"
            echo "Publishing architectonic@$VER; registry currently has ${CURRENT:-none}"
          fi

      - name: Publish
        if: steps.registry.outputs.already_published != 'true'
        run: npm publish --access public

      - name: Verify registry
        if: steps.registry.outputs.already_published != 'true'
        run: |
          VER="${{ steps.registry.outputs.package_version }}"
          for i in $(seq 1 12); do
            v="$(npm view architectonic version 2>/dev/null || true)"
            if [ "$v" = "$VER" ]; then
              echo "OK: architectonic@$v"
              exit 0
            fi
            sleep 5
          done
          echo "npm view architectonic did not show $VER (last: $v)"
          exit 1
